<!-- @Study:ST-013 -->
<!-- @Study:ST-011 -->
# دليل المساهمة في مشروع Modamoda Invisible Mannequin
## Contributing Guidelines

**الإصدار:** 1.0 | **التاريخ:** 3 نوفمبر 2025

---

## 📋 **قواعد المساهمة الأساسية**

### ⚠️ **إلزامي قبل البدء:**
1. **اقرأ الوثائق الأساسية** - لا تبدأ أي عمل قبل قراءة:
   - `PROJECT_SETUP_ANALYSIS.md`
   - `studies/MASTER_STUDIES_INDEX.md`
   - `governance/active/GOVERNANCE_FRAMEWORK.md`

2. **تشغيل فحص الامتثال** - تأكد من الامتثال 90%+:
   ```bash
   python scripts/compliance_checker.py
   ```

3. **فهم نظام الحوكمة** - جميع التغييرات يجب أن تمر بنظام الحوكمة

---

## 🔄 **عملية المساهمة**

### 📝 **الخطوات المطلوبة:**

#### 1. **إعداد البيئة المحلية**
```bash
# استنساخ المشروع
git clone <repository-url>
cd modamoda

# إعداد البيئة
python -m venv venv
source venv/bin/activate
pip install poetry
poetry install

# إعداد قاعدة البيانات المحلية
docker-compose up -d postgres redis
```

#### 2. **إنشاء فرع العمل**
```bash
# إنشاء فرع جديد
git checkout -b feature/your-feature-name

# أو للإصلاحات
git checkout -b fix/issue-description

# أو للوثائق
git checkout -b docs/update-documentation
```

#### 3. **التطوير مع الامتثال**
```bash
# فحص الامتثال قبل البدء
python scripts/compliance_checker.py

# تطوير الكود مع اتباع المعايير
# كتابة الاختبارات
# تحديث التوثيق

# فحص الامتثال أثناء التطوير
python scripts/compliance_checker.py
```

#### 4. **الاختبار والتحقق**
```bash
# تشغيل الاختبارات
poetry run pytest

# فحص جودة الكود
poetry run black --check .
poetry run flake8 .
poetry run mypy .

# فحص الامتثال النهائي
python scripts/compliance_checker.py
```

#### 5. **إنشاء Pull Request**
```bash
# تأكد من الالتزام بجميع المعايير
git add .
git commit -m "feat: add new feature

- Description of changes
- Compliance check results: 95%
- Tests added: yes
- Documentation updated: yes"

# إرسال التغييرات
git push origin feature/your-feature-name
```

### 📋 **متطلبات Pull Request:**

#### ✅ **يجب تضمين:**
- [ ] **وصف واضح** للتغييرات
- [ ] **نتائج فحص الامتثال** (90%+)
- [ ] **اختبارات** للكود الجديد
- [ ] **تحديث التوثيق** إذا لزم الأمر
- [ ] **مراجع للدراسات** ذات الصلة

#### 🔍 **قالب Pull Request:**
```markdown
## 📋 وصف التغييرات
[وصف ما تم إضافته أو تعديله]

## ✅ فحص الامتثال
- امتثال عام: [XX]%
- جودة الكود: [Grade]
- تغطية الاختبارات: [XX]%

## 🧪 الاختبارات
- [ ] اختبارات الوحدة
- [ ] اختبارات التكامل
- [ ] اختبارات الأداء

## 📚 التوثيق
- [ ] API documentation
- [ ] Code comments
- [ ] README updates

## 🔗 المراجع
- الدراسات ذات الصلة: [links]
- متطلبات الحوكمة: [references]
```

---

## 🎯 **معايير الجودة**

### 📊 **المعايير الإلزامية:**
- **امتثال للدراسات:** 90%+
- **تغطية الاختبارات:** 90%+
- **جودة الكود:** A grade (black, flake8, mypy)
- **أمان:** A+ rating (bandit, safety)
- **أداء:** P95 < 500ms

### 🚫 **ممنوع:**
- ❌ إرسال كود بدون اختبارات
- ❌ تغييرات بدون فحص امتثال
- ❌ كود غير موثق
- ❌ انتهاك معايير الأمان
- ❌ تغييرات بدون مراجع للدراسات

---

## 🏗️ **إرشادات التطوير**

### 🏛️ **معمارية النظام:**
```
src/
├── backend/          # FastAPI application
│   ├── main.py      # نقطة البداية
│   ├── config.py    # إعدادات التطبيق
│   ├── database.py  # إعدادات قاعدة البيانات
│   ├── models/      # نماذج البيانات (SQLAlchemy)
│   ├── routes/      # نقاط النهاية API
│   ├── services/    # منطق العمل
│   └── utils/       # أدوات مساعدة
├── frontend/         # Next.js application
└── shared/           # الكود المشترك
```

### 🧪 **معايير الاختبار:**
```python
# مثال على اختبار
def test_user_registration():
    """اختبار تسجيل مستخدم جديد"""
    # ترتيب (Arrange)
    test_data = {"email": "test@example.com", "password": "secure123"}

    # فعل (Act)
    response = client.post("/api/v1/users/", json=test_data)

    # تحقق (Assert)
    assert response.status_code == 201
    assert response.json()["email"] == test_data["email"]
```

### 📝 **معايير التوثيق:**
```python
def process_image(image_data: bytes, model_type: str) -> dict:
    """
    معالجة الصورة باستخدام الذكاء الاصطناعي

    Args:
        image_data: بيانات الصورة بتنسيق bytes
        model_type: نوع الموديل ('invisible' أو 'human')

    Returns:
        قاموس يحتوي على:
        - processed_image_url: رابط الصورة المعالجة
        - processing_time: وقت المعالجة بالثواني
        - confidence_score: درجة الثقة في النتيجة

    Raises:
        ValueError: في حالة بيانات غير صحيحة
        ProcessingError: في حالة فشل المعالجة

    Note:
        هذه الدالة تضمن دقة 100% للحفاظ على تفاصيل الملابس
        كما هو محدد في الدراسات الفنية.
    """
```

---

## 🔧 **أدوات التطوير**

### 🐍 **Python Development:**
```bash
# تفعيل البيئة
poetry shell

# تشغيل الخادم المحلي
uvicorn src.backend.main:app --reload

# تشغيل الاختبارات
pytest tests/ -v --cov=src

# فحص الجودة
black src/
flake8 src/
mypy src/
```

### 🐳 **Docker Development:**
```bash
# بناء الصورة
docker build -t modamoda .

# تشغيل البيئة الكاملة
docker-compose up

# تشغيل الاختبارات في Docker
docker-compose -f docker-compose.test.yml up --abort-on-container-exit
```

### 🤖 **أدوات الحوكمة:**
```bash
# فحص الامتثال
python scripts/compliance_checker.py

# مراقبة الحوكمة
python scripts/governance_monitor.py

# تقرير يومي
python scripts/governance_reporter.py daily
```

---

## 🚨 **إجراءات الطوارئ**

### 🔴 **في حالة انخفاض الامتثال:**
1. **توقف التطوير فوراً**
2. **راجع أسباب انخفاض الامتثال**
3. **أصلح المشاكل المكتشفة**
4. **أعد فحص الامتثال**
5. **احصل على موافقة من لجنة الحوكمة**

### 🟡 **في حالة اكتشاف ثغرة أمنية:**
1. **لا تلتزم بالكود المشبوه**
2. **أبلغ فريق الأمان فوراً**
3. **أزل الكود من المستودع**
4. **راجع جميع التغييرات الأخيرة**
5. **أجرِ فحص أمان شامل**

---

## 📞 **الحصول على المساعدة**

### 💬 **قنوات التواصل:**
- **Slack:** #development, #governance, #security
- **Email:** dev-support@modamoda.com
- **Issues:** استخدم GitHub Issues للأخطاء والتحسينات

### 📚 **الموارد المفيدة:**
- [`PROJECT_SETUP_ANALYSIS.md`](PROJECT_SETUP_ANALYSIS.md) - دليل الإعداد
- [`studies/MASTER_STUDIES_INDEX.md`](studies/MASTER_STUDIES_INDEX.md) - دليل التوجيه
- [`governance/active/GOVERNANCE_FRAMEWORK.md`](governance/active/GOVERNANCE_FRAMEWORK.md) - نظام الحوكمة

---

## 🎯 **المسؤولية والالتزام**

### 🤝 **التزامات المساهم:**
- **الجودة أولاً** - لا تقلل من معايير الجودة
- **الأمان أولوية** - لا تخاطر بأمان المستخدمين
- **الامتثال إلزامي** - لا تنتهك قواعد الحوكمة
- **الشفافية مطلوبة** - وثق جميع القرارات والتغييرات

### 🏆 **المكافآت:**
- **التقدير** للمساهمات عالية الجودة
- **الفرص** للعمل على مشاريع أكبر
- **التعلم** من خلال المراجعات والتغذية الراجعة
- **النمو** المهني والتقني

---

**شكراً لمساهمتك في بناء منصة Modamoda Invisible Mannequin! 🙏**

**الالتزام بالمعايير يضمن نجاح المشروع وتميزه في السوق! 🌟**
