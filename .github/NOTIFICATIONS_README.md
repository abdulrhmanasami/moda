# 🔄 إعداد التنبيهات التلقائية (Slack/Discord)

## نظرة عامة
تم تفعيل نظام التنبيهات التلقائية لإشعار الفريق بنتائج الفحوصات الحوكمية والتغطية.

## الميزات المفعلة

### 1. تعليق تلقائي على الـPR
- **متى:** عند فتح PR أو تحديثه
- **المحتوى:**
  - نتائج التغطية (Coverage)
  - نتائج الفحص الامتثالي (Compliance)
  - حالة كل فحص مع التفاصيل

### 2. تنبيهات Discord
- **متى:** بعد كل فحص (نجح أو فشل)
- **المحتوى:**
  - نوع الفحص (PR أو Push)
  - حالة النجاح/الفشل
  - رابط للتفاصيل
  - معلومات الفرع والـcommit

### 3. تنبيهات Slack
- نفس محتوى Discord لكن بصيغة Slack blocks
- أزرار تفاعلية للوصول السريع

## إعداد الـWebhooks

### Discord Webhook
```bash
# في إعدادات الريبو → Secrets and variables → Actions
# أضف سر جديد:
DISCORD_WEBHOOK=https://discord.com/api/webhooks/YOUR_WEBHOOK_ID/YOUR_WEBHOOK_TOKEN
```

### Slack Webhook
```bash
# في إعدادات الريبو → Secrets and variables → Actions
# أضف سر جديد:
SLACK_WEBHOOK=https://hooks.slack.com/services/YOUR_SLACK_WEBHOOK_URL
```

## كيفية الحصول على Webhook URLs

### Discord:
1. افتح Discord server
2. انتقل للإعدادات → Integrations → Webhooks
3. أنشئ webhook جديد
4. انسخ الـURL

### Slack:
1. انتقل لـ https://YOUR_WORKSPACE.slack.com/apps
2. ابحث عن "Incoming Webhooks"
3. أنشئ webhook جديد
4. انسخ الـURL

## اختبار النظام

### 1. اختبار التعليق على الـPR
```bash
# أنشئ فرع تجريبي وPR
git checkout -b test-notifications
echo "# Test" >> test_file.md
git add test_file.md
git commit -m "test: notifications"
git push origin test-notifications
gh pr create --title "Test Notifications" --body "Testing automated notifications"
```

### 2. اختبار التنبيهات
- راقب قناة Discord/Slack المحددة
- يجب أن تتلقى تنبيه بعد دقائق

## استكشاف الأخطاء

### لا توجد تنبيهات؟
- تأكد من إعداد الـsecrets بشكل صحيح
- تحقق من صلاحيات الـwebhook
- راقب logs الـworkflow في Actions tab

### التعليقات لا تظهر على الـPR؟
- تأكد من أن الـworkflow يعمل على PR events
- تحقق من صلاحيات الـGitHub token

## الأمان
- الـwebhook URLs محفوظة كـsecrets مشفرة
- لا يمكن الوصول لها من خارج الريبو
- يتم إرسال التنبيهات فقط عند الفشل أو النجاح
